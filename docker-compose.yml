services:
  streaming-server:
    image: alfg/nginx-rtmp
    container_name: streaming-server
    ports:
      - "1935:1935"
      - "8080:8080"
    environment:
      RTMP_PORT: 1935
      HTTP_PORT: 8080
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf.template
      - ./data:/opt/data
